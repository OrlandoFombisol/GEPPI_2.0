// v2.0 — Seed migrado a SQL en Supabase (ver supabase/migrations/001_schema.sql)
// La función se mantiene como no-op para no romper el import en App.jsx
export async function initSeedData() {
  return { omitido: true }
}
export default initSeedData
